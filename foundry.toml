[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
solc = '0.7.6'

[rpc_endpoints]
aurora = "${AURORA_RPC_URL}"

[etherscan]
aurora = { key = "${AURORASCAN_API_KEY}" }

# See more config options https://github.com/foundry-rs/foundry/tree/master/config